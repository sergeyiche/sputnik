[pytest]
testpaths = tests
pythonpath = .
markers =
    functional: end-to-end API checks
    requires_gigachat: needs GIGACHAT_AUTHORIZATION_KEY and network
    requires_knowledge: needs indexed Chroma collection
filterwarnings =
    ignore::DeprecationWarning
